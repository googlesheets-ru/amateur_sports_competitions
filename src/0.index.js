function run() {
  const app = new App();
  app.scrapeNewLinks();
  app.scrapeNextLink();
  app.scrapeNextLink();
  app.scrapeNextLink();
  app.scrapeNextLink();
  app.scrapeNextLink();
  app.scrapeNextLink();
  app.scrapeNextLink();
  app.scrapeNextLink();
  app.scrapeNextLink();
}
